<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplicatio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Mygoods" android:label="내상품"/>
        <activity android:name=".Category" android:label="카테고리"/>
        <activity android:name=".Detail" android:label="상품 상세페이지"/>
        <activity android:name=".Goods_upload" android:label="상품올리기"/>
        <activity android:name=".Main" android:label="메인화면"/>
        <activity android:name=".Participant" android:label="참가자리스트"/>
        <activity android:name=".Search" android:label="검색화면"/>
        <activity android:name=".Signup" android:label="회원가입"/>
        <activity android:name=".SmallCatePage" android:label="회원가입"/>
        <activity android:name=".Searchpage" android:label="검색후 화면"/>
    </application>

</manifest>